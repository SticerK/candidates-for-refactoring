class DaoCacheConfigProperties {
  alive: number; // alive cache time in days
  elements: number; // maximum elements in cache
}

// нет модификатора доступа private, constructor, getter(для чтения) и setter в случае, если нужно изменить значение 
